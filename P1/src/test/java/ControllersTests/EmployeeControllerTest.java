package ControllersTests;
/*

import controllers.EmployeeController;
import models.Response;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import javax.servlet.http.*;

import java.io.*;
import java.io.PrintWriter;


import static org.junit.jupiter.api.Assertions.assertTrue;
import static org.mockito.Mockito.*;


public class EmployeeControllerTest {

    EmployeeController employeeController;
    HttpServletRequest request;
    HttpServletResponse response;

    @BeforeEach
    void setup(){
        employeeController = EmployeeController.getInstance();
        request = new HttpServletRequest;
        response =
    }


    @Test
    public void testAllRequests() throws IOException {
        when(request.getParameter("username")).thenReturn("jack");
        when(request.getParameter("password")).thenReturn("password");

        StringWriter stringWriter = new StringWriter();
        PrintWriter out = new PrintWriter(stringWriter);
        when(response.getWriter()).thenReturn(out);
        employeeController.getAllRequests(request,response);
        assertTrue(stringWriter.toString().contains("Returned Lists"));

    }
}
*/
